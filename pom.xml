<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dk.statsbiblioteket.sbprojects</groupId>
        <artifactId>sbprojects-parent</artifactId>
        <version>3</version>
    </parent>

    <groupId>dk.statsbiblioteket.mediespring</groupId>
    <artifactId>yousee-workflow</artifactId>
    <version>0.2-SNAPSHOT</version>

    <scm>
        <connection>scm:git:git://github.com/statsbiblioteket/SB-yousee-workflow.git</connection>
        <url>scm:git:git://github.com/statsbiblioteket:SB-yousee-workflow.git</url>
        <developerConnection>scm:git:git://github.com/statsbiblioteket/SB-yousee-workflow.git</developerConnection>
    </scm>

  <repositories>
    <repository>
      <id>sbprojects-nexus</id>
      <url>https://sbprojects.statsbiblioteket.dk/nexus/content/groups/public</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencies>
        <dependency>
            <groupId>com.googlecode.json-simple</groupId>
            <artifactId>json-simple</artifactId>
            <version>1.1.1</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>dk.statsbiblioteket.mediaplatform.ingest</groupId>
            <artifactId>ingest_initiator_media_files</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>dk.statsbiblioteket.mediaplatform</groupId>
            <artifactId>bitrepository-url-client</artifactId>
            <version>0.2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.2-beta-5</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/assembly.xml</descriptor>
                    </descriptors>
                    <attach>true</attach>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.4</version>
                <!--
                                <configuration>
                                    <skipIfEmpty>true</skipIfEmpty>
                                </configuration>
                -->
            </plugin>
        </plugins>
    </build>
  <profiles>

  <profile>
    <id>deploy2test</id>
    <build>
      <plugins>
        <plugin>
          <artifactId>maven-antrun-plugin</artifactId>
          <version>1.7</version>
          <executions>
            <execution>
              <phase>pre-integration-test</phase>
              <configuration>
                <target>
                  <!-- Requires that a private/public key login has been enabled for this account -->
                  <scp file="target/yousee-workflow-${project.version}.zip"
                       toDir="yousee@canopus:~/ci/sb-yousee-workflow"
                       keyfile="${user.home}/.ssh/id_rsa" />
                  <sshexec host="canopus"
                           username="yousee"
                           keyfile="${user.home}/.ssh/id_rsa"
                           command="unzip ~/ci/sb-yousee-workflow/yousee-workflow-${project.version}.zip"/>
                </target>
              </configuration>
              <goals>
                <goal>run</goal>
              </goals>
            </execution>
          </executions>
          <dependencies>
            <dependency>
              <groupId>org.apache.ant</groupId>
              <artifactId>ant-jsch</artifactId>
              <version>1.8.3</version>
            </dependency>
          </dependencies>
        </plugin>
      </plugins>
    </build>
  </profile>
    <profile>
      <id>runIntegrationTest</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.7</version>
            <executions>
              <execution>
                <phase>integration-test</phase>
                <configuration>
                  <target>
                    <sshexec host="canopus"
                             username="yousee"
                             keyfile="${user.home}/.ssh/id_rsa"
                             commandResource="target/test-classes/scripts/runIntegrationTest.sh" />
                  </target>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
            <dependencies>
              <dependency>
                <groupId>org.apache.ant</groupId>
                <artifactId>ant-jsch</artifactId>
                <version>1.8.3</version>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
